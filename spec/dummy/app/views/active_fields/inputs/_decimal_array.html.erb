<%=
  render partial: "shared/array_input", locals: {
    form: form,
    name: active_field.name,
    value: active_value&.value,
    field_method: :number_field,
    field_opts: { min: active_field.min, max: active_field.max, step: 0.1 },
  }
%>
