<%= form.hidden_field :name, value: active_value.active_field.name %>

<div class="form-input">
  <%= form.label :value, active_field.name %>
  <%= form.number_field :value, { value: active_value.value, min: active_field.min, max: active_field.max, step: "any" } %>
</div>
